{
  "name": "oce-network-store-cloudflare",
  "private": true,
  "version": "7.0.0",
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy ."
  },
  "devDependencies": {
    "wrangler": "^4.30.0"
  }
}
